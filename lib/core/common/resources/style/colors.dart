import 'package:flutter/material.dart';

abstract class AppColors {
  const AppColors();

  /// Primary color of the application
  static const primaryColor = Color(0xff006175);

  /// Background Color
  static const whiteColor = Color(0xffffffff);

  /// Color used in text border and so on
  static const blackColor = Color(0xff000000);

  /// Surface color for fields
  static const surfaceColor = Color(0xffF2F2F2);

  /// color for the text buttons
  static const actionTextColor = Color(0xffC4C4C4);

  /// color for danger
  static const redColor = Color(0xffEE2323);
}
